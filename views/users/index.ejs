<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
</head>
<style>
    table {
      font-family: arial, sans-serif;
      border-collapse: collapse;
      width: 40%;
    }
    
    td, th {
      border: 1px solid #979797;
      text-align: left;
      padding: 8px;
    }
    
    tr:nth-child(even) {
      background-color: #dddddd;
    }
</style>
<body>
    <h1>All Users</h1>
    <p>
        <a href="/users/new">Add new user</a>
    </p>
    <div>
        <table>
            <tr>
                <th>S.No</th>
                <th>Name</th>
                <th>Gender</th>
                <th>Action</th>
            </tr>
            <% for ( let user of users ) {%>
                <tr>
                    <td><%= user._id %></td>
                    <td><a href="/users/<%= user._id %>"><%= user.name %></a></td>
                    <td><%= user.gender %></td>
                    <td><a href="/users/<%= user._id %>/edit"><button>Edit</button></a>   
                        <a href=""><form style='display: inline;' action="/users/<%= user._id %>?_method=DELETE" method=POST>
                        <button>Delete</button>
                    </form></a></td>
                </tr>
            <%}%>
        </table>
    </div>
</body>
</html>